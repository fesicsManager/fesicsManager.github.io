# fesicsExternal / milddle3GeoAdv 정밀 검증 보고서

- 검증일: 2026-09-21
- 대상 폴더: `G:\\내 드라이브\\Misodle Software\\fesicsExternal\\milddle3GeoAdv`
- 대상: HTML 실험 파일 4개 전수
- 검증 범위: HTML·JavaScript 문법, 핵심 계산식, 단위, 표시값, 정답 판정, 과학 개념, 조작 흐름, 안내 문구, 자습 노트 링크

## 종합 결과

| 등급 | 개수 | 의미 |
|---|---:|---|
| A 긴급 | 0 | 실행 불가, 정답이 전면 반대이거나 핵심 계산이 붕괴하는 오류 |
| B 중요 | 4 | 과학 오개념 또는 오답 판정 가능성이 있어 수정 권고 |
| C 보완 | 4 | 표현·모형의 한계·분류 명칭을 더 정확히 제시할 필요 |

네 파일 모두 JavaScript 구문 오류는 발견되지 않았고, 기본 실험 기록·표·검증 탭·퀴즈의 연결 구조도 소스상 정상입니다. 다만 실제 브라우저 엔진이 설치되지 않은 검증 환경이어서 화면 크기별 시각 배치와 모든 애니메이션의 실시간 렌더링은 정적 코드 검증으로 대체했습니다.

## 파일별 결과

### 1. 02_altitude_pressure_lab.html

**판정: B — 중요 수정 권고**

정상 확인:

- 표준대기 근사식 `P = 1013.25 × (1 − 2.25577×10⁻⁵h)^5.25588`의 구현은 정상입니다.
- 코드 계산값은 0 m 약 1,013 hPa, 5,500 m 약 506 hPa, 8,849 m 약 314 hPa, 10,000 m 약 264 hPa로 내부 설명과 일치합니다.
- 해수면 대비 비율, 그래프, 기록표 및 1,013 ÷ 3 ≈ 338 hPa 문항의 판정 범위는 정상입니다.
- 여객기 객실을 약 2,400 m 상당 기압으로 유지한다는 설명은 교육용 근사로 적절합니다.

발견 사항:

1. **[B] “기압은 5,500 m 올라갈 때마다 절반”이라는 일반화가 코드의 식과도 맞지 않습니다.**
   - 0 → 5,500 m에서는 약 1,013 → 506 hPa로 거의 절반입니다.
   - 그러나 5,500 → 11,000 m에서는 약 506 → 226 hPa로, 253 hPa가 아니라 약 45%가 됩니다.
   - 표준대기에서는 온도가 고도에 따라 달라지므로 동일한 고도 증가마다 정확히 절반이 되는 규칙은 성립하지 않습니다.
   - 현재 문구: “기압은 5,500m 올라갈 때마다 절반이 됩니다.”
   - 권장 문구: “해수면에서 약 5,500m까지 올라가면 기압이 대략 절반이 됩니다. 이후에도 계속 감소하지만, 같은 5,500m마다 정확히 절반이 되는 것은 아닙니다.”

2. **[C] 물의 끓는점 식 `100 − h/285`는 교육용 선형 근사임을 결과 화면에도 명시하는 편이 안전합니다.**
   - 현재 원리 설명에는 근사임이 드러나지만 계기판에는 단정적으로 표시됩니다.
   - 권장: “물의 끓는점(단순 근사)” 또는 “약 ○℃(교육용 근사)”로 표기.

3. **[C] 5,500m 기준선 설명의 “정확히 절반”을 “거의 절반”으로 통일해야 합니다.**
   - 코드 계산값도 약 505.9 hPa이므로 1,013.25 hPa의 정확한 절반과 미세하게 다릅니다.

### 2. 05_isobar_wind_lab.html

**판정: B — 중요 수정 권고**

정상 확인:

- 기압 경도 `4/d × 100 hPa/100km` 계산은 맞습니다.
- 교육용 모형 `풍속 = 3320/d`에서 300 km → 11.1 m/s, 150 km → 22.1 m/s, 75 km → 44.3 m/s, 500 km → 6.6 m/s가 정확히 계산됩니다.
- “간격 × 풍속 = 일정” 분석과 150 km 예측 정답 약 22 m/s의 판정은 해당 단순 모형 안에서 정상입니다.
- 실제 바람이 위도·마찰·곡률·밀도에 따라 달라진다는 단서가 포함되어 있습니다.

발견 사항:

1. **[B] 화면의 화살표가 저기압 중심을 향해 직선으로 들어가지만, 계산은 ‘지균풍 근사’이고 본문은 바람이 등압선과 거의 나란히 분다고 설명합니다.**
   - 코드 주석과 애니메이션은 “오른쪽 → 왼쪽(저기압 쪽)” 직선 흐름입니다.
   - 같은 파일의 설명은 전향력 때문에 실제 바람이 등압선과 거의 나란하거나 비스듬히 분다고 안내합니다.
   - 이 상태에서는 학생이 “지균풍도 저기압 중심으로 곧장 분다”고 받아들일 수 있습니다.
   - 수정안 A: 현재 직선 화살표를 “기압 경도력”으로 명확히 표기하고 풍속 애니메이션과 분리.
   - 수정안 B: 북반구 저기압 주위의 반시계 방향 접선 흐름으로 바람 화살표를 변경하고, 지면 마찰이 있는 경우에만 중심 쪽으로 약간 기울게 표현.

2. **[B] ‘폭풍·태풍급’ 등급명이 서로 다른 분류를 섞습니다.**
   - 코드에서는 25 m/s 이상을 모두 “폭풍·태풍급”으로 표시합니다.
   - “폭풍”은 풍력 계급 표현에 가깝고, “태풍”은 열대저기압의 구조와 최대풍속 등으로 분류하므로 일반 일기도상의 한 지점 풍속만으로 동일하게 붙이기 어렵습니다.
   - 권장: 단계명을 “매우 강함(25 m/s 이상)”으로 바꾸고, 태풍 프리셋에는 “교육용 예시이며 실제 태풍 강도 판정값이 아님”을 추가.

3. **[C] ‘세기를 정하는 가장 큰 요인은 여전히 등압선 간격’은 너무 단정적입니다.**
   - 권장 문구: “같은 위도와 비슷한 마찰 조건에서는 등압선 간격이 바람 세기를 판단하는 중요한 단서입니다.”

### 3. 06_apparent_absolute_lab.html

**판정: B — 중요 수정 권고**

정상 확인:

- 등급 밝기 변환 `10^(-0.4Δm)`, 5등급 차이 = 100배 관계가 정확합니다.
- 10 pc = 약 32.6광년, 거리 지수 `m − M = 5 log₁₀(d/10pc)` 설명이 정확합니다.
- 태양·시리우스·리겔·북극성·데네브·프록시마의 제시 거리와 `m − M`은 반올림 범위에서 서로 일치합니다.
- 겉보기 밝기와 절대 밝기 순위 정렬, 10 pc보다 가까우면 `m − M < 0`, 멀면 `m − M > 0` 분석이 정상입니다.

발견 사항:

1. **[B] 태양 문항이 +31.5도 정답으로 받아들입니다.**
   - 질문은 “두 등급의 차이(`m − M`)”를 묻고 있으므로 정답은 `−26.7 − (+4.8) = −31.5`입니다.
   - 현재 판정식은 `Math.abs(Math.abs(v)-31.5)<=0.6`이어서 `+31.5`와 `−31.5`를 모두 정답 처리합니다.
   - 수정 권장: `Math.abs(v - (-31.5)) <= 0.6`.
   - 양의 값도 허용하려면 질문을 “차이의 크기 |m − M|”로 바꾸고 정답을 31.5로 통일해야 합니다.

2. **[C] 절대 등급을 반복해서 ‘진짜 밝기’라고만 표현하면 광도와 등급의 차이가 흐려질 수 있습니다.**
   - 권장: “같은 거리(10 pc)에서 비교한 밝기, 별의 고유 광도를 비교하는 지표”로 첫 등장 시 정의.

### 4. 09_hubble_age_lab.html

**판정: 통과(보완 권고)**

정상 확인:

- `v = 2,000 × d`에서 d의 단위가 억 광년으로 일관됩니다.
- 2억 광년 → 4,000 km/s, 6억 광년 → 12,000 km/s, 1,000 km/s → 0.5억 광년 계산이 맞습니다.
- `1광년 = 9.4607×10¹² km`, `1년 = 3.1557×10⁷ s` 변환과 `거리/속도 ≈ 150억 년` 계산이 맞습니다.
- 실제 허블 상수 약 70 km/s/Mpc의 허블 시간이 약 140억 년이고 정밀 우주 나이가 약 138억 년이라는 구분이 포함되어 있습니다.
- 가까운 은하의 고유운동, 허블 긴장, 팽창 역사의 영향 등 모형의 한계가 잘 제시되어 있습니다.

발견 사항:

1. **[C] 실험의 비례상수는 약 65 km/s/Mpc이므로 실제 대표값 70과 다르다는 점을 결과 화면 가까이에 표시하면 더 명확합니다.**
   - 1억 광년당 2,000 km/s ≈ 65.2 km/s/Mpc입니다.
   - 현재 원리 탭에는 실제 값과 차이가 설명되지만 탐구 화면에서는 모델값이 실제 정밀값처럼 보일 수 있습니다.
   - 권장: “이 실험은 계산을 쉽게 하기 위해 약 65 km/s/Mpc로 단순화한 모형” 문구 추가.

## 우선 수정 순서

1. `06_apparent_absolute_lab.html`의 `m − M` 정답 판정식 수정
2. `05_isobar_wind_lab.html`의 바람 화살표 방향과 지균풍 설명 통일
3. `02_altitude_pressure_lab.html`의 “5,500m마다 절반” 문구 수정
4. `05_isobar_wind_lab.html`의 “폭풍·태풍급” 분류명 수정
5. 나머지 C급 보완 문구 반영

## 최종 의견

이 폴더는 이전의 단순 형식 검사만으로는 놓치기 쉬운 **모형 내부의 과학적 불일치 3건과 정답 판정 오류 1건**이 확인되었습니다. 특히 등압선 파일은 계산식 자체보다 애니메이션 방향이 설명과 충돌하는 것이 핵심 문제이며, 절대등급 파일은 양수 답안을 오답으로 걸러내지 못하는 실제 채점 오류입니다. 위 B급 4건을 수정한 뒤 재검증하는 것이 좋습니다.
