# 정밀검토보고서 지적 반영 기록 — 2026-09-23

- 대상: `_정밀검토보고서_고1통합과학_추가실험_19-28 / 29-38 / 39_260922.md` 3편
- 반영 범위: **전부** (계산 오류 + 개념 오류 + 표현 보완)
- 수정 방식: 생성기 소스(`fesicsExternal/_labgen_src/integ_*.js`)를 고쳐 실험 21개를 재생성 → 배포 → 자습 노트 시드(`notes/_src/seeds/ig_0*.json`) 수정 → 노트 재생성 → CTA 재주입
- 수정 전 백업: `backup/high1Integ_검토수정전_260923/` (실험 21개)

## 1. 검토 보고서에 대한 판정

지적 30여 건을 코드와 대조한 결과 **사실과 다른 지적은 없었습니다.** 다만 두 가지는 등급·범위가 어긋났고, 한 가지는 보고서가 오히려 과소평가했습니다.

| 구분 | 내용 |
|---|---|
| 과장 | 23번 요약표가 "결과 카드의 이온 전하 표기가 항상 +, −"라고 했으나 실제로는 **'전자 배치' 한 줄만** 잘못됨(그림·'만든 이온' 줄은 정확) |
| 과장 | 33번 '긴급' 등급. 원리 탭 「실험 모형과 실제의 차이」에 CO₂ 환산 가정이 이미 적혀 있어 **표기 불일치 수준** |
| **과소평가** | **38번.** 보고서는 "높은 정확도에 도달할 수 있다"고 조심스럽게 썼으나, 실제로 돌려 보니 **실험의 핵심 주장 자체가 성립하지 않았음**(아래 3절) |

## 2. 고친 계산·표시 오류

| 번호 | 문제 | 수정 |
|---|---|---|
| 36 | `Math.round(dmT / 1e6) + ' 만 t/s'` → 428만 t을 **4만 t**으로 표시(100배 오류) | `/1e4`로 수정. 확인: **428 만 t/s** |
| 39 | `off = max(0, hits − 1)` → 20염기 비표적 기대값 0.0056을 **0으로 표시** | `off = (2N − 1) × p`, `hits = 1 + off`로 분리. 확인: **0.006곳**, 정밀도 99.44 % |
| 39 | `cut = 1 − exp(−(k+1)×0.8)` → 허용 불일치가 커지면 표적 절단 효율이 자동 상승(근거 없음) | **절단 효율 항목 삭제.** 원리 탭에 "절단 효율은 안내 RNA·PAM·세포 조건이 정하는 별개 문제"라고 명시 |
| 35 | 비구름 중심이 계산값(110°E)과 그림(140°E 고정)이 불일치 | 두 곳을 같은 식 `min(200, max(120, 140 + (1−w)×60))`으로 통일 |
| 27 | 지권 표시가 `4000 − released×0.8`이라 **숲을 벤 탄소까지 화석 연료에서 빠진 것처럼** 그려짐. 조림 시 "배출 합계 −55 GtC" 표시 | `fossil`과 `land`를 분리해 지권은 `fossil`만 반영. 결과·표·기록을 "화석 누적 / 숲(+방출·−흡수)"로 나눔 |
| 32 | 반응식 계수 누락 (`Mg + Ag⁺ → Mg²⁺ + Ag`) | 산화수로 계수를 자동 계산. 확인: **Mg + 2Ag⁺ → Mg²⁺ + 2Ag** |
| 23 | 전자 배치 줄에 전하 하드코딩 → Mg²⁺가 **Mg⁺**로 표시 | 전하에 따라 ⁺/²⁺/³⁺, ⁻/²⁻로 생성 |
| 30 | "다섯 번의 대멸종"인데 자료는 4개 | **트라이아스기 말**(201 Ma, 종 80 %, 과 22 %) 추가 → 5대 대멸종 완성. 노트 표도 갱신 |
| 33 | 반응식은 CO를 만드는데 결과는 "배출 이산화 탄소" | 라벨을 **"배출 탄소를 CO₂로 환산"**으로 바꾸고, 그림에 `CO → (공기 중 산소와 만나) CO₂`와 `2CO + O₂ → 2CO₂` 가정을 표시 |
| 21 | 용량을 1024로 나누고 `KB`로 표기(실제는 KiB) | **1000으로 나눠** KB 표기와 일치시키고 "1 KB = 1,000 B" 명시 |

## 3. 38번 — 실험을 다시 설계했습니다

보고서의 지적대로인지 확인하려고 학습 알고리즘을 그대로 60번씩 돌려 봤습니다.

**수정 전 (귀 특징이 고양이 0.55~1.0 / 개 0~0.45로 완벽히 분리)**

```
흰 고양이 100 %, 200장, 20회 → 전체 98.0 %  흰 100 %  검은 92.3 %
                  1000장으로  → 검은 98.8 %
                  500회 학습  → 검은 99.2 %
```

귀만으로 완벽히 갈리므로 학습기가 결국 귀를 찾아냅니다. 노트에 쓴 "검은 고양이 매우 낮음", "전체 정확도 절반 가까이", "자료를 늘려도 편향은 고쳐지지 않는다"가 **모두 사실이 아니었습니다.**

**수정 후 (귀를 겹치게: 고양이 0.45~1.0 / 개 0~0.55)**

```
공정 50 %  → w1 −0.1  w2 18.1 | 전체 90.8 %  흰 91.1 %  검은 91.8 %  (차이 0.7 %p)
편향 90 %  → w1  3.0  w2 17.4 | 전체 89.1 %  흰 94.8 %  검은 76.5 %  (차이 18.3 %p)
편향 100 % → w1  5.2  w2 16.4 | 전체 86.2 %  흰 95.2 %  검은 63.5 %  (차이 31.7 %p)
편향 100 % · 1,000장 → 검은 69.9 %  (자료를 늘려도 남음)
```

전체 정확도는 4.6 %p밖에 안 떨어지는데 검은 고양이만 무너지는 — 의도했던 교육 메시지가 그대로 나옵니다. 함께 고친 것:

- **시험 자료 400장을 고정 씨앗으로 생성** → 조건을 바꿔도 같은 문제로 채점(보고서 지적)
- 검증 문구를 **기록에서 계산해 생성**하도록 변경(무조건 표시되던 단정 제거)
- 노트의 표를 실측값으로 전면 교체

## 4. 표현 보완 (단정 완화·가정 명시)

- **22** 이온화 에너지: Mg→Al, P→S 예외를 명시하고 "대체로"로 수정
- **24** 규산염: `SiO₄` 등을 **"규산염 골격의 단위식"**으로 정정하고, 양이온까지 넣은 전하 균형식(Mg₂SiO₄ 등)을 새 항목·새 열로 추가. "대표 광물" → "대표 광물(계열)"
- **25** 도체 전류 88만 A에 **"이론값"** 표시, 도체·절연체 선택 시 도핑 설정이 계산에 쓰이지 않음을 안내
- **26** "전류가 아무리 커도 손실 0" → **임계 전류·임계 자기장** 조건 추가. 납의 "1913년 노벨상" 문구를 "1911년 수은에서 초전도 발견 뒤 확인된 초기 초전도체"로 정정
- **28** 변성 비가역성: "실행할 때마다 새 감자즙"임을 명시. 효소량–속도의 인과를 **"활성 부위 수가 2배"**로 정정
- **29** 지층 대비: "같은 시기에 쌓였다" → "비슷한 시기에 쌓였을 가능성이 크다", 재퇴적 가능성 언급
- **31** 품종 1을 **"병원체가 노리도록 적응한 기준 품종"**으로 표현하고, 결과에 다양성 효과와 저항성 효과가 섞여 있음을 명시. "1→2가 효과 최대"를 **기록에서 계산**해 생성
- **34** 알렌·베르그만 규칙이 예외 있는 **경향**임을 명시
- **35** "엘니뇨 지수" → **"동태평양 수온 편차(단순 지수)"**, "판정" → "모형 판정", 실제 ONI의 3개월 이동 평균 조건 안내
- **36** "서울 시민 하루 전기" 비교를 근거 있는 값(**4인 가구 월 300 kWh 기준 약 8만 가구 한 달치**)으로 교체. L ∝ M³·⁵가 주계열성 경험식임은 기존에 명시되어 있었음
- **37** "모두 물을 끓여 증기 터빈" → **복합 화력은 가스 터빈 + 증기 터빈**, 지열은 방식이 여러 가지임을 명시
- **39** "1/4 감소"는 완전 일치에서만 정확함을 명시, 기대값을 확정 개수처럼 쓰던 표현 정정, "비표적 0곳 확인" → "검출 한계가 있어 위험이 0이라고 단정할 수 없음"

## 5. 검증

| 항목 | 결과 |
|---|---|
| 인라인 JS 구문·중복 id (실험 57개 전체) | 57/57 통과 |
| 브라우저 자동 실행 (새 실험 21개 × 6조건) | **오류 0 · 경고 0** |
| 자습 노트 구조·링크 전수 검사 | 21/21 통과 |
| `validate.js` | bad = 0 |
| 실험의 자습 노트 CTA | 21/21 주입 |
| 값 육안 확인 | 36번 428만 t/s · 39번 비표적 0.006곳 · 32번 `Mg + 2Ag⁺ → Mg²⁺ + 2Ag` |

## 6. 남은 것

- `index.html`에 새 실험 21개가 아직 등록되어 있지 않아 `📝 노트` 배지가 붙지 않습니다(기존 18개는 그대로).
- 압축 파일 `_고1통합과학_추가실험21개_260922.zip`은 수정 전 버전이므로 다시 만들어야 합니다.
