# fesicsExternal / zerolabMathHigh 상세 추가 검증 보고서

- 검증일: 2026-09-14
- 대상: `zerolabMathHigh` HTML 68개
- 판정 요약: 오류 3개 / 개선 권고 6개 / 특이사항 없음 59개

## 1. 검사 범위

68개 파일 전부에 대해 다음을 확인했다.

1. 핵심 수학 원리와 예시 계산
2. 공식의 정의역·성립 조건·중근 처리
3. 인터랙티브 입력 범위와 계산 코드의 대응
4. JavaScript 구문, 중복 id, 외부 의존
5. viewport·문서 언어·입력 label·canvas 대체 설명

## 2. 자동·구조 검사 결과

| 검사 항목 | 결과 |
|---|---:|
| 검사 HTML | 68 |
| JavaScript 구문 오류 | 0 |
| 중복 id 파일 | 0 |
| viewport 누락 | 0 |
| html lang 누락 | 0 |
| 외부 CDN·원격 리소스 의존 | 0 |
| 이름 없는 입력 컨트롤 | 0 |
| 입력의 min/max 밖 기본값 | 0 |

라디오 입력은 `for` 속성 대신 `<label><input ...>선택지</label>` 구조로 연결되어 있어 접근성 누락으로 판정하지 않았다. 실행을 막는 공통 템플릿 오류는 발견되지 않았다.

## 3. 오류 — 수정 필요

### 3.1 `hs_c1_12_cubic_root_count_CommonMath1_Quad_Ep04.html`

- 현재: “실근은 1개 또는 3개”, “실근이 정확히 2개일 수는 없어요(중근을 따로 세지 않는 한)”
- 문제: 서로 다른 실근을 세면 삼차방정식은 실근이 정확히 2개일 수 있다. 페이지 자체의 예 `(x−1)²(x+2)=0`도 서로 다른 실근은 1과 −2, 두 개다.
- 괄호 설명도 반대로 되어 있다. 중근을 중복해서 세면 실근은 3개이고, 중근을 한 번만 세면 2개다.
- 권장 수정:
  - “중복도를 포함하면 실근은 1개 또는 3개이고 전체 복소근은 3개”
  - “서로 다른 실근의 개수는 1개·2개·3개가 가능”
  - 표의 `실근 수`가 중복도 포함인지 서로 다른 근인지 명시

### 3.2 `hs_calc_55_increase_extremum_Calculus1_DerivApp_Ep02.html`

- 본문은 `|x|`의 뾰족점처럼 미분 불가능한 극값을 올바르게 설명한다.
- 그러나 오해 설명에서 “f′=0은 필요조건일 뿐”이라고 일반화해 앞 문장과 충돌한다.
- 일반적으로 극값의 필요조건은 아니다. 내부점에서 미분 가능하다는 가정이 있을 때만 페르마 정리에 의해 `f′(a)=0`이 필요하다.
- 권장 수정: “함수가 a에서 미분 가능하고 a가 정의역의 내부점인 경우, 극값이면 f′(a)=0이다. 하지만 역은 성립하지 않는다.”

### 3.3 `hs_stat_64_conditional_probability_ProbStat_CondProb_Ep02.html`

- 현재: `P(B|A)=P(A∩B)/P(A)`, `P(A∩B)=P(A)P(B|A)`, `P(B|A)=P(B)`이면 독립
- 문제: 조건부확률은 `P(A)>0`일 때만 정의된다. 베이즈 공식도 `P(B)>0` 조건이 필요하다.
- 현재 조절 범위에서는 분모가 0이 되지 않더라도 수학 원리 설명에는 정의 조건을 밝혀야 한다.
- 권장 수정: 각 공식에 `P(A)>0`, 베이즈 공식에 `P(B)>0`을 명시한다.

## 4. 개선 권고

### 4.1 `hs_stat_68_sample_mean_ci_ProbStat_Estim_Ep02-04.html`

- `x̄±1.96σ/√n`은 모표준편차 σ를 알고 있고 표본평균이 정규분포를 따르는 상황에서 정확한 95% 신뢰구간이다.
- 코드는 정규난수에서 독립 표본을 뽑으므로 계산은 맞지만, 핵심 원리에도 “정규모집단·σ를 아는 경우”라고 조건을 붙이는 것이 좋다.
- 일반 모집단의 작은 표본이나 σ 미지 상황에서는 t구간 등 다른 처리가 필요하다.

### 4.2 `hs_alg_38_sine_wave_Algebra_Trig_Ep06.html`

- 현재 주기: `2π/b`
- 일반식은 `b≠0`일 때 `2π/|b|`이다. `b=0`이면 상수함수이고 기본주기가 정해지지 않는다.
- 실험 입력이 양수 b만 허용한다면 “이 실험에서는 b>0”을 제목 또는 원리 문장에 명시하면 된다.

### 4.3 `hs_c2_28_rational_asymptote_CommonMath2_Func_Ep07.html`

- `y=(ax+b)/(cx+d)`의 점근선을 언제나 `x=-d/c`, `y=a/c`라고 표현한다.
- `c≠0` 및 `ad-bc≠0` 조건이 필요하다. 분자와 분모가 비례하면 약분 뒤 상수함수가 되고, 제외점은 구멍이지 세로 점근선이 아니다.
- 실험 본체의 `k/(x-p)+q`는 k가 0이 아닌 값만 사용하므로 정상이다.

### 4.4 `hs_stat_67_binomial_normal_ProbStat_Dist_Ep06-07.html`

- 근사 조건 `np≥5`, `n(1-p)≥5`을 제시한 점은 좋다.
- 이산 이항확률을 연속 정규분포로 근사할 때 `k±0.5` 연속성 수정을 사용하면 수치 비교가 더 정확하다는 설명을 추가하면 좋다.

### 4.5 `hs_stat_59_circular_permutation_ProbStat_Perm_Ep01.html`

- “목걸이는 `(n−1)!/2`”는 서로 다른 구슬 n개이고 n≥3이며 뒤집기를 같은 것으로 보는 기본 상황의 식이다.
- 같은 색 구슬이 있거나 추가 대칭이 있으면 단순히 2로 나눌 수 없으므로 조건을 붙이는 것이 안전하다.

### 4.6 `hs_alg_30_exponent_laws_Algebra_ExpLog_Ep02.html`

- 양의 밑에서는 실수 지수법칙 설명이 적절하다.
- “m, n에 무엇을 넣어도”는 범위가 너무 넓게 읽힌다. 밑 `a>0`인 실수 지수 범위라고 문장 앞에서 먼저 제한하는 것이 좋다.
- 음의 밑과 유리수 지수에서는 정의 가능 여부 및 `(a^m)^n=a^{mn}` 적용에 주의가 필요하다.

## 5. 내용이 양호한 대표 파일

- `hs_stat_65_independent_exclusive...`: 독립과 배반, 확률 0 사건의 예외까지 정확히 구분
- `hs_stat_68_sample_mean_ci...`: 특정 구간에 모평균이 있을 확률이 95%라는 오해를 올바르게 교정
- `hs_calc_52_intermediate_value...`: 연속 조건, 부호가 같은 경우의 역 부정까지 포함
- `hs_calc_58_signed_area...`: 정적분과 실제 넓이를 구분하고 예시 계산도 정확
- `hs_c1_18_matrix_square...`: 비가환성 때문에 `(A+B)²=A²+AB+BA+B²`임을 정확히 설명
- `hs_alg_29_nth_root_count...`: 실수 범위의 n제곱근 개수를 홀짝·부호로 올바르게 분류
- `hs_alg_31_log_definition...`: 밑과 진수의 정의 조건을 정확히 명시

나머지 59개 파일은 이번 검사 기준에서 별도 오류나 조건 누락을 확인하지 못했다.

## 6. 수정 우선순위

1. **즉시**: 삼차방정식 실근 개수와 중근 설명
2. **즉시**: 극값에서 `f′=0`이 필요조건이라는 문장의 가정 추가
3. **높음**: 조건부확률·베이즈 공식의 분모 양수 조건
4. **보통**: 신뢰구간 가정, 사인함수 주기 절댓값, 유리함수 퇴화 경우
5. **낮음**: 연속성 수정, 목걸이 조건, 실수 지수의 범위 표현

## 7. 전체 추가분 상세 검증 완료 현황

| 구분 | 파일 수 | 오류 | 개선 | 양호/특이사항 없음 |
|---|---:|---:|---:|---:|
| fesicsLabs | 195 | 7 | 34 | 154 |
| 대학 3개 폴더 | 38 | 5 | 17 | 16 |
| fesicsMathLabs | 202 | 3 | 7 | 192 |
| zerolabMathHigh | 68 | 3 | 6 | 59 |
| **합계** | **503** | **18** | **64** | **421** |

이번 `zerolabMathHigh` 보고서까지 작성하면 신규 503개에 대한 폴더별 상세 판정이 모두 완료된다.
