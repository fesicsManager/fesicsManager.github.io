# 고1 공통수학Ⅰ HTML 실험 23개 검토 보고서

- 검토일: 2026-09-23
- 대상: Google Drive `high1Math1` 폴더의 `hz_*.html` 23개. `index.html`은 개수에서 제외.
- 방식: 각 HTML의 설명, 입력 범위, 계산·판정·기록·분석 JavaScript를 읽고 기본값과 경계 조건을 대조한 정적 검토. 브라우저 실사용·화면 크기별 시각 검사 및 전 입력 조합의 자동 실행은 수행하지 않았음. 따라서 아래의 ‘특이사항 없음’은 완전 무오류 판정이 아님.

## 우선 수정 사항

| 우선 | 파일 | 재현 및 근거 | 수정 제안 |
|---|---|---|---|
| 높음 | `hz_188_c1_system_quadratic_CommonMath1_Quad_Ep04.html` | 기본 `s=5`, `p=s+1=6`이면 두 근은 3, 2. 후보 `(3,2)`와 `(2,3)`은 `x+y=5`, `xy=6`을 **둘 다** 만족한다. 코드도 둘 다 `sol=true`로 판정한다. 반면 본문은 ‘짝을 바꿔 끼우면 다른 식이 어긋난다’, 화면은 잘못된 후보가 ‘한쪽만 맞음’이라고 단정한다. 대칭인 두 식에서는 순서를 바꾸어도 해다. | 현재 합·곱 식을 유지한다면 두 순서쌍이 모두 해임을 설명하고 `(r1,r1)`, `(r2,r2)`만 조건별 검산 대상으로 삼는다. 짝 바꾸기 오개념을 다루려면 `x+y=s`와 **비대칭** 두 번째 식을 사용하고 후보·해설을 함께 다시 설계한다. |
| 높음 | `hz_021_c1_zero_matrix_CommonMath1_Matrix_Ep06.html` | 코드가 `A=[[a,b],[a,b]]`, `B=[[c,d],[-c,-d]]`를 생성하므로 `AB`의 모든 성분은 `(a-b)c`, `(a-b)d`이다. 화면의 ‘B의 두 열은 부호만 반대’는 틀림: **두 행**이 부호만 반대다. 또한 본문은 A의 행과 B의 열이 수직이면 0이라고 하지만 제시한 행렬에서 그 조건은 `a=b`(또는 `c=d=0`)임을 명시해야 한다. | ‘B의 두 행은 부호만 반대’로 고치고, `a=b≠0`, `(c,d)≠(0,0)`이면 두 행렬이 0이 아니면서 `AB=O`임을 실험 힌트로 제시한다. |
| 중간 | `hz_012_c1_always_positive_CommonMath1_Quad_Ep08.html` | `st()`의 ‘실제 최솟값’은 `x=-6..6` 표본의 최소다. `a<0`이면 전체 실수에서 최솟값은 없으며, `a>0`이어도 꼭짓점이 범위 밖이면 표본 최소와 진짜 최솟값이 다르다. `a=0`에서는 이차식이 아닌데 기록/분석에 같은 판정 조건이 들어간다. | 꼭짓점 `-b/(2a)`에서의 값 `c-b²/(4a)`을 `a>0`일 때만 ‘실제 최솟값’으로 표시하고, `a≤0`은 ‘최솟값 없음’ 또는 별도 사례로 처리한다. 표시 구간의 샘플 최소는 ‘화면 구간 표본 최소’로 구분한다. |
| 중간 | `hz_003_c1_factor_theorem_CommonMath1_Poly_Ep07.html` | 입력 `p,q`에 0이 허용된다. 생성식 `(x-p)(x-q)(x+1)`의 상수항은 `pq`라서 `p=0` 또는 `q=0`이면 0. 본문의 ‘정수근은 상수항의 약수’와 분석의 `P.c[3]%(k||1)` 약수 판정은 0의 약수라는 표현과 `k=0` 처리에서 부정확해진다. | 상수항 0일 때 먼저 `x`를 인수로 묶고 남은 다항식의 정수근 후보를 찾게 한다. `k=0`을 별도로 처리하고 ‘상수항의 약수’ 문구에 상수항이 0이 아닌 경우라는 조건을 붙인다. |
| 중간 | `hz_001_c1_degree_count_CommonMath1_Poly_Ep01.html` | 설명은 ‘0은 차수를 정하지 않는다’고 하지만 네 지수 입력 모두 0이 가능하다. 이는 0다항식이 아니라 상수항 두 개를 더한 꼴이며, 계수에 따라 상수 2개가 될 수 있다. ‘0은 차수를 정하지 않는다’는 표현은 수 0과 영다항식을 혼동시킨다. | ‘영다항식의 차수는 정하지 않는다’로 명확히 하고, 모든 지수가 0인 경우 상수 다항식의 차수 0을 별도로 안내한다. |
| 낮음 | `hz_005_c1_imaginary_unit_CommonMath1_Complex_Ep01.html` | 본문에서 실수 제곱의 증명 문장이 `x>0`, `x 0`, `x=0`으로 나타나 `x<0` 조건이 누락되어 보인다. | 원문 HTML의 수식 표기에서 `x&lt;0`이 정상 표시되는지 확인하고, 빠져 있다면 `x<0이면 x²>0`을 복구한다. |

## 파일별 판정

| 번호 | 주제 | 정적 검토 결과 |
|---|---|---|
| 001 | 다항식 차수 | 설명 문구 수정 권장(위 참조) |
| 002 | 세 항의 제곱 | 계산 원리·입력 범위에서 뚜렷한 충돌 없음 |
| 003 | 인수정리 | 상수항 0 경계 처리 필요 |
| 004 | 고차식 인수분해 | 계산 원리·입력 범위에서 뚜렷한 충돌 없음 |
| 005 | 허수 단위 | 증명 문장 표기 확인 필요 |
| 006 | 음수 제곱근 곱 | 계산 원리·입력 범위에서 뚜렷한 충돌 없음 |
| 007 | 켤레와 순서 | 계산 원리·입력 범위에서 뚜렷한 충돌 없음 |
| 008 | 근의 조건 검산 | 계산 원리·입력 범위에서 뚜렷한 충돌 없음 |
| 009 | 포물선과 x축 | 계산 원리·입력 범위에서 뚜렷한 충돌 없음 |
| 010 | 연립부등식 | 계산 원리·입력 범위에서 뚜렷한 충돌 없음 |
| 011 | 절댓값 부등식 | 계산 원리·입력 범위에서 뚜렷한 충돌 없음 |
| 012 | 항상 양수 | 최솟값 표기·퇴화 경계 수정 필요 |
| 013 | 합의 법칙과 곱의 법칙 | 계산 원리·입력 범위에서 뚜렷한 충돌 없음 |
| 014 | 순열 | 계산 원리·입력 범위에서 뚜렷한 충돌 없음 |
| 015 | 이웃하는 배열 | 계산 원리·입력 범위에서 뚜렷한 충돌 없음 |
| 016 | 이웃하지 않는 배열 | 계산 원리·입력 범위에서 뚜렷한 충돌 없음 |
| 017 | 여사건 활용 | 계산 원리·입력 범위에서 뚜렷한 충돌 없음 |
| 018 | 행렬의 상등 | 계산 원리·입력 범위에서 뚜렷한 충돌 없음 |
| 019 | 행렬의 덧셈 | 계산 원리·입력 범위에서 뚜렷한 충돌 없음 |
| 020 | 행렬의 곱셈 | 계산 원리·입력 범위에서 뚜렷한 충돌 없음 |
| 021 | 영행렬의 곱 | 화면 힌트 오류, 조건 보강 필요 |
| 187 | 순열·조합 혼합 | 계산 원리·입력 범위에서 뚜렷한 충돌 없음 |
| 188 | 연립이차방정식의 짝 | 설명과 실제 해 판정 충돌 |

## 검토 범위와 후속 확인

압축파일 이름으로 요청받았지만 Drive 검색에서 해당 ZIP 자체는 찾지 못했다. 같은 주제의 `high1Math1` 폴더에 있는 23개 HTML을 직접 검토했다. 원래 ZIP의 파일 목록과 완전히 동일한지는 비교하지 못했다. 각 파일의 브라우저 콘솔 오류, 애니메이션 중 상태, 모바일 잘림, 색 대비, 외부 리소스 접근성은 실행 검사를 거쳐야 한다. 우선 188과 021의 수학적 설명을 수정하고 012·003의 경계값을 재검증하기 바란다.
