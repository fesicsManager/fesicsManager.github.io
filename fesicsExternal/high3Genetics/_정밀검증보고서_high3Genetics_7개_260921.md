# high3Genetics 웹 실험 파일 정밀 검증 보고서

- 검증일: 2026-09-21
- 대상 폴더: `G:\내 드라이브\Misodle Software\fesicsExternal\high3Genetics`
- 대상: HTML 실험 7개 + 구성 파일 `units.json` 1개
- 검증 범위: HTML/JavaScript 구조, 계산 로직, 유전학 개념, 결과 문구, 표·그림의 일치, 접근성 기본 구조, 폴더 구성

## 1. 종합 판정

**조건부 부적합 — 수업 배포 전에 핵심 오류 수정이 필요합니다.**

HTML 7개 모두 JavaScript 구문 오류, 중복 ID, 끊어진 `for`/`aria-controls` 참조는 발견되지 않았습니다. `units.json`에도 실제 파일 7개가 빠짐없이 등록되어 있습니다. 그러나 과학적 의미 또는 시각 결과를 잘못 전달하는 **A급 5건**, 결과 해석을 흐리는 **B급 8건**, 표현·모형 보완이 필요한 **C급 5건**이 확인됐습니다.

| 등급 | 의미 | 건수 |
|---|---|---:|
| A | 결과가 사실과 다르거나 핵심 학습 개념을 오개념으로 만들 수 있음 | 5 |
| B | 특정 선택·표현에서 판정이 과도하거나 계산/설명이 부정확함 | 8 |
| C | 정확도·가독성·교육적 안전성을 높이기 위한 보완 | 5 |

### 우선 수정 순서

1. `07_chromosome_mutation...` 핵형 그림의 성염색체 개수·터너/클라인펠터 표시
2. `06_human_pedigree...` 불충분한 가계도를 단일 유전 방식으로 확정하는 로직
3. `02_transcription_translation...` 종결 코돈을 아미노산 수에 포함하는 로직
4. `04_mendel_cross...` 동형 접합자의 배우자를 `AA`, `aa`로 만드는 로직
5. `07_chromosome_mutation...` DNA 염기 서열 분석이 모든 이상을 찾는다는 판정

## 2. 파일별 검증 결과

### 01_dna_replication_lab_Genetics_Ep05_Ep07.html

**판정: 대체로 적합, 문구 보완 필요**

정상 확인:

- ¹⁵N 배지에서 ¹⁴N 배지로 옮긴 뒤 반보존적 복제 비율은 0~3세대에서 올바르게 계산됩니다.
- 1세대는 중간 DNA 100%, 2세대는 중간 50%·가벼운 DNA 50%, 3세대는 중간 25%·가벼운 DNA 75%입니다.
- 단일 가닥 φX174를 포함해 샤가프 법칙의 예외를 비교하도록 구성한 점은 적절합니다.

| 등급 | 위치 | 문제 | 수정 제안 |
|---|---|---|---|
| B | 228행 | “어떤 생물이든 A=T, G=C”라고 단정하지만 같은 파일에서 단일 가닥 φX174를 예외로 사용합니다. | “**이중 가닥 DNA에서는 일반적으로** A≈T, G≈C”로 수정합니다. |
| C | 472행 | 샤가프 법칙 판정을 `|A−T|<2`, `|G−C|<2`라는 임의 기준으로 처리하지만 화면에 허용 오차 설명이 없습니다. | “실측값의 반올림·측정 오차를 고려해 차이가 2%p 미만이면 근사적으로 일치”라고 밝힙니다. |
| C | 445~446행 | 일부 조성값 합계가 99.9% 또는 반올림 차이를 보입니다. 오류는 아니나 학생이 100%가 아닌 이유를 질문할 수 있습니다. | “반올림 때문에 합계가 100%와 약간 다를 수 있음”을 주석에 추가합니다. |

### 02_transcription_translation_lab_Genetics_Ep08-09.html

**판정: 수정 필요**

정상 확인:

- DNA 주형 가닥에서 mRNA를 만드는 상보적 전사 규칙은 맞습니다.
- 삽입·결실 뒤 읽는 틀이 바뀌는 기본 로직은 맞습니다.
- 코돈표의 64개 코돈 대응은 구문상 빠짐없이 정의되어 있습니다.

| 등급 | 위치 | 문제 | 재현 예 | 수정 제안 |
|---|---|---|---|---|
| A | 477, 537행 | 번역 함수가 종결 코돈을 목록에 먼저 넣고 `n=list.length`로 계산하여 **종결을 아미노산 1개로 셉니다.** | 유전자 A 정상: `AUG-GCC-UUC-AAA-CGG-UAA`를 “아미노산 6개”로 표시하지만 실제 폴리펩타이드는 5개입니다. | 종결 코돈은 표시용 항목으로만 두고 아미노산 수와 폴리펩타이드 서열에서는 제외합니다. |
| B | 461행 | “7번째 염기를 T로 치환”은 유전자 C의 7번째 염기가 이미 T라서 실제 변화가 전혀 없습니다. 그런데 침묵 돌연변이처럼 보일 수 있습니다. | 유전자 C + 해당 치환 | 유전자별 원래 염기와 다른 염기를 사용하거나, 변화 없음은 “동일 염기로 지정되어 돌연변이 없음”으로 별도 판정합니다. |
| B | 485~488행 | 모든 치환 결과를 “종결 생성” 또는 “아미노산 하나가 바뀜”으로만 분류합니다. 기존 종결 소실, 개시 코돈 손상, 실제 무변화가 구분되지 않습니다. | 현재 데이터에서는 무변화 사례가 실제 발생 | 정상·침묵·과오·무의미·종결 소실·개시 손상·실제 무변화를 구분합니다. |
| C | 509행 부근 | 결실된 염기는 화면에 존재하지 않는데 `pos`의 다음 염기를 빨간색으로 칠해 결실 위치가 그 염기의 돌연변이처럼 보입니다. | 모든 결실 선택 | 삭제 위치에 빈칸/삼각표시를 두고 이후 염기는 틀 이동 색으로 구분합니다. |

### 03_gene_regulation_lab_Genetics_Ep10.html

**판정: 핵심 발현량은 적합, 생장 판정 수정 필요**

정상 확인:

- 정상형에서 젖당 존재·포도당 부재일 때 최대 발현, 포도당 존재 시 낮은 발현이라는 방향은 맞습니다.
- `lacI⁻`, `Oᶜ`, 구조 유전자 고장의 기본 효과는 교육용 단순화로 사용할 수 있습니다.
- 알로락토스가 억제 단백질에 결합한다는 설명이 들어가 있습니다.

| 등급 | 위치 | 문제 | 수정 제안 |
|---|---|---|---|
| B | 454행 | 생장 상태를 젖당 분해 효소량만으로 판정하여 **포도당이 있는 배지에서도 “천천히 자람” 또는 “젖당이 없어 관계없음”**으로 표시합니다. 포도당이 있으면 대장균은 포도당을 이용해 생장할 수 있습니다. | 생장과 lac 발현을 분리합니다. 예: 포도당 있음 → “포도당으로 잘 자람, lac 발현 낮음”; 포도당 없음·젖당 있음·효소 정상 → “젖당으로 자람”. |
| B | 233, 445~453행 | `lacI⁻`와 `Oᶜ`를 둘 다 “항상 켜짐”으로만 처리해 cis/trans 차이를 가르치지 못합니다. 단일 오페론 모형에서는 가능하지만 설명 없이 일반화됩니다. | “이 모형은 오페론 1개만 둔 단순화이며, Oᶜ는 같은 DNA 분자의 구조 유전자에만 작용”이라고 덧붙입니다. |
| C | 238행 | 설명식은 `[억제 해제 ? 1 : 0.01] × [포도당 없음 ? 100 : 5]`인데 실제 코드는 억제 시 `0.02`를 사용합니다. | 문서식과 코드 중 하나로 통일합니다. 현재 코드를 따르면 `0.02`가 맞습니다. |

### 04_mendel_cross_lab_Genetics_Ep01.html

**판정: 수정 필요**

정상 확인:

- Aa×Aa, AaBb×AaBb의 배우자 조합과 표현형 분류는 기본적으로 맞습니다.
- 무작위 표본 수 증가에 따라 관찰 비가 기대 비에 가까워지는 활동 의도는 적절합니다.
- 자유도 1과 3에 쓰는 3.84, 7.81 임계값은 5% 수준의 근삿값으로 적절합니다.

| 등급 | 위치 | 문제 | 재현 예 | 수정 제안 |
|---|---|---|---|---|
| A | 451~452행 | 단성 잡종 동형 접합자의 배우자를 한 대립유전자 `A` 또는 `a`가 아니라 문자열 `AA`, `aa`로 반환합니다. | `AA × aa` 퍼넷 사각형과 내부 유전자형이 `AAaa`로 생성됩니다. | `g[0]===g[1] ? [g[0]] : [g[0],g[1]]`로 수정합니다. |
| B | 446, 474~477행 | `AA×aa` 기대값 `[1,0]`에 카이제곱 검정을 적용합니다. 기대도수가 0인 범주가 있어 일반적인 카이제곱 적합도 검정 조건을 만족하지 않습니다. 현재 함수는 그 항을 건너뛰어 항상 χ²=0으로 보이게 합니다. | `AA × aa` 선택 | 이 교배에서는 카이제곱을 “적용하지 않음”으로 표시하거나, 변이가 있는 교배만 검정합니다. |
| C | 247행 | 귓불·보조개를 멘델 법칙의 대표 적용처럼 나열하면 단일 유전자 형질로 오해하기 쉽습니다. | 문구에서 삭제하거나 “과거 단순 예시로 소개됐지만 실제로는 복합적”이라고 보완합니다. |

### 05_gene_linkage_lab_Genetics.html

**판정: 대체로 적합, 판정 기준 보완 필요**

정상 확인:

- 재조합형 개체 수/전체 개체 수로 재조합 빈도를 계산하는 로직은 맞습니다.
- 50% 부근에서는 다른 염색체인지 같은 염색체에서 매우 먼지 구별하기 어렵다는 설명은 적절합니다.
- A–B 12, B–C 16, A–C 28이라는 지도 순서는 내부적으로 일치합니다.

| 등급 | 위치 | 문제 | 수정 제안 |
|---|---|---|---|
| B | 468~469행 | 관찰 재조합률이 42% 이하이면 무조건 “연관”, 초과면 “독립”으로 판정합니다. 표본이 100개일 때 독립인 짝도 우연히 42% 이하가 나올 수 있어 단일 실행에서 오판합니다. | “연관 가능/독립과 구별 어려움”처럼 확률적 문구를 쓰고, 반복 자료 또는 통계 검정을 사용합니다. |
| B | 468행 및 결과표 | 모든 구간에서 `재조합 빈도(%) = 지도 거리(cM)`로 출력합니다. 같은 파일 설명은 이 근사가 약 10 cM 이내에서만 정확하다고 밝히므로 A–C 28%를 28 cM로 단정하는 결과와 충돌합니다. | 10% 초과에서는 “근사 지도 거리”로 표시하거나 지도함수(Haldane/Kosambi)를 선택적으로 제공합니다. |
| C | 504행 | 50% 근처에서도 숫자 `cM`를 계속 표시해 거리를 실제로 정할 수 있는 것처럼 보입니다. | 독립 판정 시 지도 거리를 “결정 불가”로 표시합니다. |

### 06_human_pedigree_lab_Genetics_Ep02.html

**판정: 수정 필요**

파일 도입부에 “가계도만으로 유전 양식이 하나로 정해지지 않을 수 있다”는 주의가 있으나 실제 활동은 불충분한 자료로 답을 확정합니다.

| 등급 | 위치 | 문제 | 수정 제안 |
|---|---|---|---|
| A | 452~457, 526~527행 | 가계도 ‘나’는 세대마다 발현자가 있다는 이유만으로 “우성”, ‘다’는 발현자가 모두 남자라는 이유만으로 “반성 열성”으로 확정합니다. 제시된 작은 가계도만으로는 상염색체/성염색체 여부를 단정할 수 없습니다. | 각 가계도에 필요한 부모-자녀 연결과 성별 전파 단서를 추가하거나 결과를 “가능한 유전 양식” 복수로 표시합니다. |
| A | 480~490, 527행 | 사용자가 고른 형질과 가계도 판정이 서로 독립입니다. 예를 들어 ABO 혈액형을 선택해도 가계도 ‘다’는 “반성 열성”로 출력하면서 제목에는 ABO식 혈액형을 함께 표시합니다. | 가계도별 형질을 고정하거나, 선택한 형질에 맞는 가계도 데이터·판정 로직을 별도로 사용합니다. |
| B | 447행 | 귓불 모양을 “분리형이 우성”인 단순 상염색체 우성 형질로 제시합니다. 실제 사람의 귓불 형태는 단일 유전자 우열로 깔끔하게 나뉘지 않는 복합 형질입니다. | 해당 예시를 교과서적으로 확실한 단일 유전자 질환/형질로 교체하거나 복합 형질임을 명시합니다. |
| B | 가계도 그림 전체 | 세대별 모든 인물을 한 부모쌍의 자녀처럼 연결해 보여 주지만, 3세대의 정확한 부모 관계가 데이터에 없습니다. 실제 가계도 규칙과 다른 연결선이 생깁니다. | 개체별 배우자·부모 ID를 가진 가계도 자료구조로 바꾸고 그 관계만 연결합니다. |

### 07_chromosome_mutation_lab_Genetics_Ep04.html

**판정: 우선 수정 필요**

정상 확인:

- 감수 1분열 비분리 시 4개 모두 비정상, 감수 2분열 비분리 시 2개 정상·2개 비정상이라는 결과는 맞습니다.
- 염색체 수 이상, 구조 이상, 단일 유전자 돌연변이를 구분하려는 활동 목표는 적절합니다.

| 등급 | 위치 | 문제 | 재현/영향 | 수정 제안 |
|---|---|---|---|---|
| A | 487~500행 | 핵형 그림이 1~22, X, Y의 **24종을 모두 기본 2개씩** 그립니다. 정상부터 X 2개와 Y 2개가 동시에 표시되며 총 48개처럼 보입니다. 터너는 Y 하나만 줄이고 X 두 개를 남기며, 클라인펠터는 X 세 개와 Y 두 개처럼 표시됩니다. | 다운·터너·클라인펠터 등 모든 핵형 그림이 성염색체 수준에서 틀립니다. | 상염색체 1~22는 쌍으로, 성염색체는 사례에 따라 XX/XY/X/XXY만 그리도록 별도 배열로 구성합니다. |
| A | 451, 473행 | `DNA 염기 서열 분석`을 선택하면 염색체 수 이상·큰 구조 이상·유전자 돌연변이를 모두 무조건 “찾음”으로 판정합니다. 일반적인 염기 서열 분석이 모든 이수성과 구조 이상을 자동 검출한다는 뜻이 되어 부정확합니다. | 검사 선택 활동의 핵심 결론이 잘못됩니다. | 검사 항목을 `핵형`, `염색체 마이크로어레이`, `표적 유전자/염기서열 검사`로 나누고 이상 유형별 검출 가능성을 설정합니다. |
| B | 444행 | 다운 증후군 핵형 표기가 `47, XY(또는 XX) + 21`로 되어 있습니다. | 표준 표기와 다릅니다. | `47,XX,+21 또는 47,XY,+21`로 수정합니다. |
| B | 247행 | NIPT와 양수 검사를 함께 “염색체 수 이상을 확인”한다고 표현합니다. NIPT는 선별검사이며 확진검사가 아닙니다. | 산전 검사에 대한 오해 가능 | “NIPT로 위험도를 선별하고, 필요 시 양수검사 등 진단검사로 확인”으로 수정합니다. |
| C | 443~449행 | 빈도 수치는 출처·지역·산모 연령 조건 없이 고정값으로 제시됩니다. 특히 다운 증후군 빈도는 산모 연령 등에 따라 달라집니다. | 값 자체보다 일반화가 문제 | “대략적인 전체 출생 기준이며 집단·연령에 따라 다름”을 표시하거나 출처를 붙입니다. |

## 3. 공통 기술 검증

| 항목 | 결과 |
|---|---|
| HTML 문서 선언 및 `lang="ko"` | 7개 모두 정상 |
| 인라인 JavaScript 구문 검사 | 7개 모두 통과 |
| 중복 HTML ID | 없음 |
| `for`, `aria-controls` 대상 누락 | 없음 |
| `units.json` 등록 파일과 실제 HTML | 7개 모두 일치 |
| 외부 라이브러리 의존 | 없음; 단일 HTML로 구성 |

주의: 이번 검증은 내려받은 원본 코드의 정적 구조·계산 경로와 가능한 선택 조합을 대조한 결과입니다. 현재 실행 환경에는 Playwright용 브라우저 실행 파일이 없어 실제 브라우저 픽셀 렌더링·모바일 터치 실기 검사는 수행하지 못했습니다. 따라서 수정 후에는 Chrome에서 1280×720, 768×1024, 390×844 세 화면 크기로 최종 동작 검사를 권장합니다.

## 4. 권장 재검증 체크리스트

- [ ] 유전자 A 정상 번역 결과가 “아미노산 5개 + 종결”로 표시되는가
- [ ] 유전자 C의 모든 ‘치환’이 실제로 원래 염기와 다른가
- [ ] `AA×aa` 퍼넷 사각형의 자손 유전자형이 모두 `Aa`인가
- [ ] 포도당이 있는 배지에서 생장 문구와 lac 발현 문구가 분리되는가
- [ ] 가계도 판정이 주어진 정보만으로 가능한 범위를 넘지 않는가
- [ ] ABO 형질 선택과 반성 열성 판정이 한 화면에서 모순되지 않는가
- [ ] 정상 핵형이 XX 또는 XY 중 하나만 표시하는가
- [ ] 터너 45,X와 클라인펠터 47,XXY 그림의 성염색체 개수가 정확한가
- [ ] NIPT가 선별검사로 표현되는가
- [ ] 독립 유전자 쌍의 재조합률이 표본 변동 때문에 42% 이하가 되어도 ‘연관’으로 확정되지 않는가

## 5. 최종 결론

`01`과 `05`는 표현·판정 기준을 보완하면 사용할 수 있고, `03`은 생장 상태 문구를 수정하면 핵심 실험은 유지할 수 있습니다. `02`, `04`, `06`, `07`은 학생에게 직접 잘못된 결과를 보여 줄 수 있으므로 **수정 전 배포를 권장하지 않습니다.** 특히 `07`의 핵형 그림은 시각적으로 명백한 오개념을 만들기 때문에 가장 먼저 고쳐야 합니다.
